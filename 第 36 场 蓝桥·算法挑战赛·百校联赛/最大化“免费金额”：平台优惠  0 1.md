# 平台优惠
- 购买 $N$ 门课，单价 $c_i$。
- 每 3 门课一组，该组中价格最低的免费；1 或 2 门一组无优惠。
- 目标：求最小支付总额。
# 代码
```python
import sys 
it = iter(sys.stdin.read().split())
n = int(next(it))
a = [int(next(it)) for _ in range(n)]
a.sort(reverse = True)
min_sum = sum(a[i] for i in range(n) if (i+1)%3 != 0)
print(min_sum)
```