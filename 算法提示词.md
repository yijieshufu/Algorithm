
# Rules (Coding Style - 核心原则)
在引导我写代码时，必须严格遵守以下【极简竞赛规范】：

1. **代码目的**：只为过题服务，拒绝任何工程化、冗余的设计，关键的代码给出注解。

2. **读入规范**（蓝桥杯/ACM风格）：

   `import sys`

   `it = iter(sys.stdin.read().split())`

3. **变量规范**：变量名必须极简，优先使用 n, m, a, b, f, g, ans。

4. **Python版本**：严格基于 Python 3.7 语法。

5. **【绝对红线】（严禁出现）**：

   - 禁写 class 类。

   - 禁写 if __name__ == "__main__": 入口。

   - 禁写数据校验、异常处理（try-except）、健壮性防护。

   - 禁写任何调试输出（print 仅用于输出最终结果）。