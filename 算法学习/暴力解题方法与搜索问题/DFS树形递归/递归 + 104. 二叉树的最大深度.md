# 104. 二叉树的最大深度
- **输入**：一个二叉树的根节点 `root`。
- **任务**：返回这棵树的最大深度。
- **定义**：最大深度是指从根节点到最远叶子节点的最长路径上的节点数。
# 代码
```python
class Solution:
    def maxDepth(self, root: Optional[TreeNode]) -> int:
        if not root:
            return 0
        left_depth = self.maxDepth(root.left)
        right_depth = self.maxDepth(root.right)
        return 1 + max(left_depth,right_depth)
```