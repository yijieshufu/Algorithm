# 斐波那契数列
- **状态转移**：根据定义 $F(n) = F(n-1) + F(n-2)$。
- **边界处理**：$F(0)=0, F(1)=1$。
- **序列对齐**：题目要求“第 $n$ 项。
# 代码

```python
import os
import sys
it = iter(sys.stdin.read().split())
n = int(next(it))
a = [0]*30 # 注意
a[0] = 0
a[1] = 1
for i in range(2,26):
  a[i] = a[i-1] +a[i-2]
print(a[n])
```
