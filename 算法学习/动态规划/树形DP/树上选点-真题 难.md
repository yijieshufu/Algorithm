# 题目
**1. 核心条件**：
- **不相邻**：选中的点集 $\{P_i\}$ 中，任意两个点不能有边直接相连（即不能是父子关系）。
- **距离互不相同**：每个选中的点到根节点（1号点）的距离（深度）必须唯一。换句话说，**每一层最多只能选一个点**。
- **权值最大**：在满足上述条件下，使得选中点的点权 $V_i$ 之和最大。  
**2. 目的**：
- 输出满足条件的最大点权和。
## 分析

