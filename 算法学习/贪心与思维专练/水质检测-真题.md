#review
# 题目描述

在一个 $2 \times n$ 的格子里，已经有一些点（检测器 `#`）了。两个点只要上下或左右相邻就是连通的。你的任务是：**最少**往空格子 `.` 里填多少个新的 `#`，能让现有的所有 `#` 都连在一起。

- **关键约束**：$n$ 高达 $1,000,000$，意味着我们只能用 $O(n)$ 的时间复杂度，也就是只能扫一遍。

- **题型**：线性动态规划 / 贪心
- **难度**：⭐⭐⭐（中等，省赛高频题）
- **是否必会**：**是**。这种 $2 \times N$ 的窄条状网格问题，是动态规划的绝佳切入点，必须掌握。






# 笔记
