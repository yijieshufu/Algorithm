#review
# 题目描述

小蓝要画一个“2025图形”，该图形是一个高为 h、宽为 w 的矩形（即 h 行 w 列的字符矩阵）。
**填充规则如下：**
- 第一行：从左到右重复填入 “2025” 字符串，直到填满 w 个字符。
- 从第二行开始：每一行相对于上一行**向左循环移动一位**（即首字符移到末尾），然后继续用 “2025” 重复填充至宽度 w。
**示例：**  
当 h = 5, w = 10 时，输出图形为：
```python
2025202520
0252025202
2520252025 
5202520252 
2025202520
```
# 分析
# 代码
```python
import os
import sys
it = iter(sys.stdin.read().split())
h = int(next(it))
w = int(next(it))
s="2025"
L=4
for i in range(h):# i相当于左移了
  row = []
  for j in range(w):
    row.append(s[(i+j)%L]) 
  print("".join(row))# 每行打印
```
# 笔记