# 题目
**题目名称**：子段异或和之和  
**核心任务**：给定一个长度为 $n$ 的数组 $A$。你需要计算所有可能的子段 $[L, R]$（其中 $1 \le L \le R \le n$）的异或和，最后把这些异或和全部加起来。  
**约束条件**：
- $n \le 10^5$（必须在线性或 $O(n \log n)$ 级别解决）。
- $A_i \le 2^{20}$（提示我们要从“位”的角度思考）。
