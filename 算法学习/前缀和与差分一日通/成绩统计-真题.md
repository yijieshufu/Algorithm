# 题目描述
### 题目简述

- **背景**：小蓝在查 $n$ 个人的成绩，查到第 $x$ 个人时，他可以从这 $x$ 个人里挑出**任意** $k$ 个人。
    
- **目标**：求最小的 $x$，使得这 $k$ 个人的成绩**方差**小于 $T$。
    
- **核心公式**：方差 $\sigma^2 = \frac{\sum_{i=1}^k (v_i - \bar{v})^2}{k}$。
    
- **数据范围**：$n, k \le 10^5$，这意味着我们的算法必须在 $O(n \log^2 n)$ 或 $O(n \log n)$ 左右。

# 公式转化

$$
\sigma^2 = \frac{k\sum v_i^2 - (\sum v_i)^2}{k^2}
$$

**题目要求** $\sigma^2$小于 $T$

$$
\frac{k\sum v_i^2 - (\sum v_i)^2}{k^2} < T
$$

$$
k \cdot \sum v_i^2 - (\sum v_i)^2 < T \cdot k^2
$$
