
# 题目描述
有的数转换为二进制之后，正好有 3 个数位为 1。例如 7 转换为二进制为 111，有 3 个数位为 1；又如 11 转换为二进制为 1011，有 3 个数位为 1。满足条件的前几个数依次为：7, 11, 13, 14, 19, 21, …… 请问，第 23 个满足条件的数是多少？
# 代码
```python
import os
import sys
# 请在此输入您的代码
res = 1
num=1
while res < 23:
  if bin(num).count('1')==3:
    res+=1
  num+=1
print(num)
```
