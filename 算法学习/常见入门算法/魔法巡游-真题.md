# 题目
**条件**：
1. **交替轮流**：小蓝先手选 $s_i$，小桥后手选 $t_j$，如此交替。
2. **下标递增**：每次选取的石头原始下标必须比前一颗**严格更大**（$i < j < k \dots$）。
3. **共鸣触发**：相邻两颗石头的数字必须包含**至少一个相同**的共鸣元素（数字 `0`、`2` 或 `4`）。  
**目的**：  
计算满足规则的巡游序列的**最大长度**。
## 分析

```python


```